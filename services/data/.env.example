# Copy to .env or .env.local in this directory (services/data). Loaded on startup.
# Restart uvicorn after changes.

# No secrets are required for the data service itself — the Next.js app holds
# ANTHROPIC_API_KEY for the chat agent. Optional tuning:

# Optional: override pybaseball / HTTP behavior if you add env-based config later.

# POST /v1/pybaseball_sandbox needs RestrictedPython installed: pip install -r requirements.txt
# ENABLE_PYBASEBALL_SANDBOX=1
